<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#99000000"> <!-- Halka transparent black background -->

    <!-- 1. PORTAL (Naye "Cone of Light" design se update kiya gaya) -->
    <ImageView
        android:id="@+id/portal_view"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="-150dp"
        android:src="@drawable/glowing_portal_cone"
        android:alpha="0.7"
        android:scaleType="fitXY" />

    <!-- 2. CARD (Naye screenshot ke hisaab se update kiya gaya) -->
    <LinearLayout
        android:id="@+id/card_view"
        android:layout_width="280dp"
        android:layout_height="400dp"
        android:layout_centerInParent="true"
        android:background="@drawable/card_background"
        android:elevation="12dp"
        android:orientation="vertical"
        android:padding="20dp"
        android:gravity="center_horizontal|center_vertical">

        <!-- Yahaan aapka file ka icon/logo aa sakta hai -->
        <ImageView
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:src="@mipmap/ic_launcher"
            android:layout_marginBottom="20dp"/>

        <TextView
            android:id="@+id/file_name_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="document_name.pdf"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            android:gravity="center"
            android:maxLines="2"
            android:ellipsize="end"/>

        <TextView
            android:id="@+id/status_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="Swipe up to send"
            android:textColor="#DDDDDD"
            android:textSize="16sp"
            android:gravity="center" />
    </LinearLayout>

</RelativeLayout>

